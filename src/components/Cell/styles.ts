import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  cellContainer: {
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: 'rgba(65, 21, 0, 1)',
    borderRadius: 16,
    borderColor: 'rgba(104, 59, 19, 1)',
    borderWidth: 1,
  },
});
